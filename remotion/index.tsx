import { registerRoot } from 'remotion';
import { Video } from './Video';
import { RemotionRoot } from './Root';

// Register both the original Video and the new Week Journey composition
registerRoot(RemotionRoot);
